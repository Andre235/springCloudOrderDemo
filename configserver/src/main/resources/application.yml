server:
  port: 8762
spring:
  application:
    name: configServer
  profiles:
    active: native #从本地读取配置文件
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared